PORT=3000
# Optional for cloud deployment (Render, Railway, etc.)
# If DATABASE_URL is provided, DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD are ignored.
# DATABASE_URL=postgresql://user:password@host:5432/database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=joebill
DB_USER=postgres
DB_PASSWORD=yourpassword
JWT_SECRET=change_this_to_a_long_random_string
